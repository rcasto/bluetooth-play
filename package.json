{
  "name": "bluetooth",
  "version": "1.0.0",
  "description": "Playing around with Bluetooth communicaton",
  "main": "index.js",
  "scripts": {
    "prestart": "babel src --presets es2015 --out-dir dist",
    "start": "node dist/scan-pi.js",
    "network": "node dist/scan-network.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Richie Casto",
  "license": "ISC",
  "dependencies": {
    "bluetooth-serial-port": "^2.1.1",
    "node-libnmap": "^0.2.15",
    "rpio": "^0.9.15"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-polyfill": "^6.22.0",
    "babel-preset-es2015": "^6.22.0"
  }
}
